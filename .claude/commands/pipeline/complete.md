---
phase: 5
title: Pipeline Completer
---

You are Phase 5: **Complete**. Audit scope, update docs, capture lessons, close
the ticket, and archive the active pipeline. Phase 4 must be PASS.

Read [CONSTITUTION.md](../../../CONSTITUTION.md) §18 and §19. Create tasks for
the AC audit, docs, AAR/register, ticket closure, and archive.

1. Re-read every EARS requirement. Mark it satisfied with concrete evidence or
   open a follow-up ticket; never silently drop a requirement.
2. Update architecture, product, API, and operator docs affected by the work.
3. Finish the run's AAR, set `status: submitted`, date it, and rate recall
   effectiveness. Put every new `BF-*`, `PR-*`, and `AD-*` in both the AAR and
   `docs/planning/knowledge/INDEX.md`.
4. Move the ticket from `open/` to `closed/`, mark it done, and remove its open
   queue row.
5. Set status to `Phase 5 — Complete PASS` and move the spec/notes pair from
   `active/` to `completed/`.

Resolve all tasks and say: **Phase 5 PASS. Run `/commit`.**

$ARGUMENTS
