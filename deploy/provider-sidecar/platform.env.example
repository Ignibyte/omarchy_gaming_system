# Public routing values only. Keep the four OGS_PROVIDER_* runtime secrets in
# the platform service's secret manager, separate from provider credentials.
OGS_PROVIDER_SIDECAR_RELEASE_ID=@EXACT_REGISTERED_RELEASE_UUID@
OGS_PROVIDER_SIDECAR_SOCKET=127.0.0.1:@PROVIDER_TLS_PORT@
